#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-02-121"
title = "Share TUI test render harness"
status = "done"
created = "2026-06-02"
started = "2026-06-02"
completed = "2026-06-02"

[content]
description = "Refactor TUI renderer tests to share the repeated TestBackend, Terminal, draw, and buffer capture harness through the existing tui::ui::test_support module. Keep per-test App setup and assertions local, and avoid adding files or splitting production TUI modules."

[[content.acceptance_criteria]]
text = "TUI test support exposes a reusable render harness for TestBackend drawing and buffer capture"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "TUI renderer tests use the shared harness while keeping test-specific setup and assertions local"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "TUI focused tests, clippy, full tests, and govctl check pass"
status = "done"
category = "chore"
