#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-02-152"
title = "Reuse dynamic test command builders"
status = "done"
created = "2026-06-02"
started = "2026-06-02"
completed = "2026-06-02"
refs = ["RFC-0002"]

[content]
description = "Replace repeated manual Vec<String> construction in move, delete, and happy-path integration tests with the existing shared command and work command builders. Keep the tests in their current files, preserve scenario-specific setup helpers, and avoid new abstractions, include-based splitting, snapshot churn, or behavior changes."

[[content.acceptance_criteria]]
text = "Move, delete, and happy-path integration tests reuse common dynamic command builders instead of manual Vec<String> command assembly"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Scenario-specific test setup helpers remain local and no new test files or include-based split is introduced"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Focused move, delete, happy-path tests plus govctl check and no-snapshot/no-include/no-journal scans pass"
status = "done"
category = "chore"
