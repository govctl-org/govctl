#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-088"
title = "Extract parse TOML IO helpers"
status = "done"
created = "2026-06-01"
started = "2026-06-01"
completed = "2026-06-01"
depends_on = ["WI-2026-06-01-087"]

[content]
description = "Extract reusable parse module helpers for repeated TOML file reading, schema validation, deterministic directory loading, and schema-header writes. Keep artifact-specific parsing functions together in the parse module for now; do not split every artifact into a separate module in this slice."

[[content.acceptance_criteria]]
text = "repeated TOML read, validate, and deserialize logic is shared by ADR, guard, work item, and release loaders"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "repeated schema-header write and dry-run preview logic is shared by ADR, guard, work item, and release writers"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "parse-related tests or cargo test and govctl check pass"
status = "done"
category = "chore"
