#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-094"
title = "Extract loop command support modules"
status = "active"
created = "2026-06-01"
started = "2026-06-01"
refs = ["RFC-0006"]

[content]
description = "Move reusable loop command support logic out of src/cmd/loop_cmd/mod.rs so the module root keeps command entrypoints while discovery, root validation, generated ID, and output formatting live behind coherent child modules."

[[content.acceptance_criteria]]
text = "Loop command module root contains command entrypoints without list rendering or state discovery helper bodies"
status = "pending"
category = "changed"

[[content.acceptance_criteria]]
text = "Loop state discovery and generated ID helpers are reusable by start, resume, and run without importing through unrelated command entrypoints"
status = "pending"
category = "changed"

[[content.acceptance_criteria]]
text = "loop command tests and govctl check pass"
status = "pending"
category = "chore"
