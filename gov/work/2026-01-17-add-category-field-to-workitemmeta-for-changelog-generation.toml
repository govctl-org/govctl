[govctl]
schema = 1
id = "WI-2026-01-17-029"
title = "Add category field to acceptance criteria for changelog generation"
status = "done"
created = "2026-01-17"
started = "2026-01-17"
completed = "2026-01-17"
refs = [
    "ADR-0013",
    "ADR-0012",
]

[content]
description = """
Add `category` field to `ChecklistItem` (acceptance criteria) for Keep a Changelog categorization.

Consolidate `ChangelogCategory` enum into `model.rs` for reuse across RFC changelog entries and work item criteria. Enable prefix parsing when adding criteria via CLI (e.g., `fix: memory leak` parses to category=fixed)."""

[[content.acceptance_criteria]]
text = "Consolidate `ChangelogCategory` enum into `model.rs` with `from_prefix` method"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Add `category` field to `ChecklistItem` with default `Added`"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "Update `add acceptance_criteria` command to parse prefixes per [[ADR-0012]]"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "Update `work.schema.toml` with category field documentation"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Update work template with category field documentation"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "All existing work items remain valid (backward compatible default)"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "All tests pass"
status = "done"
category = "fixed"
