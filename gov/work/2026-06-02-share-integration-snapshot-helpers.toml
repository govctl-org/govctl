#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-02-059"
title = "Share integration snapshot helpers"
status = "done"
created = "2026-06-02"
started = "2026-06-02"
completed = "2026-06-02"

[content]
description = "Reduce duplicated integration-test snapshot helper logic by sharing the common snapshot-name derivation and Insta settings used by split test suites. Preserve existing snapshot file names, snapshot contents, and integration test behavior without introducing new include-based test splitting."
notes = ["Full-match Insta metadata normalization is separate from this helper-sharing slice: approved snapshot files and normal assertions remain stable, but `INSTA_REQUIRE_FULL_MATCH=1` exposes source/expression metadata drift from module-based test invocation that should be handled by a dedicated snapshot metadata migration if desired."]

[[content.acceptance_criteria]]
text = "integration snapshot helper logic is shared instead of repeated across test roots"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "existing integration snapshot file names and contents remain stable"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "targeted snapshot tests, full tests, clippy, and govctl check pass"
status = "done"
category = "chore"
