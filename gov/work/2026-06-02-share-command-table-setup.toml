#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-02-163"
title = "Share command table setup"
status = "done"
created = "2026-06-02"
started = "2026-06-02"
completed = "2026-06-02"

[content]
description = "Reduce repeated command table setup by centralizing the shared UTF-8 full preset, dynamic arrangement, and bold-header construction used by command list outputs. Scope is command-output formatting helpers plus existing list/tag/loop list call sites; preserve row content, JSON/plain output, and table-visible semantics."
notes = [
    "INSTA_REQUIRE_FULL_MATCH=1 across test_happy_path currently fails on pre-existing shared-macro metadata even when snapshot bodies match; this table cleanup should not update those snapshots.",
    "INSTA_REQUIRE_FULL_MATCH=1 for test_tags also fails on pre-existing shared snapshot-helper metadata with matching snapshot bodies; standard test_tags is the relevant no-churn check for this slice.",
]

[[content.acceptance_criteria]]
text = "tag, loop, and list output tests pass without snapshot churn"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "command list table setup is shared through a reusable output helper instead of repeated table initialization"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "fmt, clippy, govctl check, and no execution-history-field/no-new-include/no-snapshot scans pass"
status = "done"
category = "chore"
