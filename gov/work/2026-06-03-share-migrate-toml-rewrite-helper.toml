#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-03-009"
title = "Share migrate TOML rewrite helper"
status = "done"
created = "2026-06-03"
started = "2026-06-03"
completed = "2026-06-03"

[content]
description = "Share the repeated TOML rewrite planning logic in migrate rewriting so single-file and directory rewrites build write operations through one local helper while preserving current behavior, ordering, and file layout."

[[content.acceptance_criteria]]
text = "TOML rewrite planning uses a shared local helper for directory, RFC, and release write operations"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "migrate rewrite behavior and operation ordering remain unchanged"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "focused migrate tests, fmt, clippy, govctl check, and no-snapshot/no-include scans pass"
status = "done"
category = "chore"
