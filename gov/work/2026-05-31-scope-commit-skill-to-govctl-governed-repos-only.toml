#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-05-31-001"
title = "scope commit skill to govctl-governed repos only"
status = "done"
created = "2026-05-31"
started = "2026-05-31"
completed = "2026-05-31"

[content]
description = """
The /commit skill, when installed globally (e.g., in ~/.codex or ~/.agents/skills),
fires in every repository — not just govctl-governed ones. During Codex goal workflows,
the agent tries to commit after each compaction, causing meaningless govctl check calls
in non-governed repos.

Fix: Add a governance detection gate (check for gov/config.toml or gov/ directory)
between Step 1 (Detect VCS) and Step 2 (Govctl Pre-Commit Checks) in the commit skill.
When the repo is not governed, skip all govctl-specific steps and fall through to a
plain VCS commit.

See: https://github.com/govctl-org/govctl/issues/16"""
notes = ["For jj-git colocated repositories, detect jj with jj root before checking git and avoid parallel VCS detection that could choose the wrong commit workflow."]

[[content.journal]]
date = "2026-05-31"
content = "Added governance detection gate (Step 1.5) to commit skill. Checks for gov/config.toml or gov/ directory. Non-governed repos skip govctl steps and do plain VCS commit."

[[content.journal]]
date = "2026-05-31"
scope = "skills-agents"
content = "Synced same fix to global skill copy at ~/.agents/skills/commit/SKILL.md. Also improved Step 1 wording to warn against parallel VCS detection (jj root + git rev-parse)."

[[content.journal]]
date = "2026-05-31"
scope = "skills-agents"
content = "Added governance detection gate (Step 1.5) to .claude/skills/commit/SKILL.md. Checks for gov/config.toml or gov/ directory before running govctl commands. If not governed, skips Steps 2-3 and 7, falls through to plain VCS commit."

[[content.journal]]
date = "2026-05-31"
content = "Synced fix to global skill copy at ~/.agents/skills/commit/SKILL.md. Also improved Step 1 to warn against parallel VCS detection."

[[content.journal]]
date = "2026-05-31"
content = "Renamed Step 1.5 to Step 1b — folded governance detection into Step 1 as sub-steps 1a (VCS) and 1b (governance). Cleaner naming, no renumbering churn."

[[content.journal]]
date = "2026-05-31"
content = "Committed fix(skills-agents): add governance detection gate to commit skill; govctl check passes"

[[content.acceptance_criteria]]
text = "commit skill detects governance before running govctl commands"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "global skill copy at ~/.agents/skills/commit matches repo-local copy"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "non-governed repos skip govctl check and work item steps"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "governed repos continue to run full govctl workflow unchanged"
status = "done"
category = "fixed"
