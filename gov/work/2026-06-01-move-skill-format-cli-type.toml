#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-145"
title = "Move skill format CLI type"
status = "done"
created = "2026-06-01"
started = "2026-06-01"
completed = "2026-06-01"
refs = ["RFC-0002:C-GLOBAL-COMMANDS"]

[content]
description = "Move the init-skills SkillFormat value enum out of src/cli/mod.rs into the existing cli/common namespace. Preserve clap parsing, command router usage, skill installation behavior, and help output while keeping the CLI root focused on the top-level command enum."

[[content.acceptance_criteria]]
text = "SkillFormat lives in the cli common module and remains available through the existing cli re-export"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "init-skills command parsing and skill installation callers remain unchanged"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "agent-dir/help tests and govctl check pass"
status = "done"
category = "chore"
