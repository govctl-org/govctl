#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-071"
title = "Consolidate TUI test fixtures"
status = "done"
created = "2026-06-01"
started = "2026-06-01"
completed = "2026-06-01"

[content]
description = "Consolidate duplicated TUI renderer test scaffolding into a single test-only support boundary so dashboard and list renderer tests share model fixtures and buffer extraction helpers without changing production rendering modules or adding runtime abstraction."

[[content.acceptance_criteria]]
text = "Dashboard and list renderer tests share one TUI test fixture helper instead of duplicating ProjectIndex and buffer extraction setup"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "Production TUI renderer module boundaries remain unchanged except for cfg(test) support wiring"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "cargo fmt, focused TUI renderer tests, cargo test, and govctl check pass"
status = "done"
category = "chore"
