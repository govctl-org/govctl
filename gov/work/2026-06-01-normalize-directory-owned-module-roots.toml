#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-075"
title = "Normalize directory-owned module roots"
status = "queue"
created = "2026-06-01"

[content]
description = "Normalize Rust modules that already have child directories by moving each current same-name root file under `src/` (for example `src/command_router.rs` and `src/model.rs`) into the matching directory-owned `mod.rs` root. Keep the work mechanical: do not create new child modules, do not split additional behavior, do not change public command/API surfaces, and leave coherent single-file modules without child directories alone."

[[content.acceptance_criteria]]
text = "All current same-name Rust file/directory module root pairs under `src/` are moved to matching directory-owned `mod.rs` roots"
status = "pending"
category = "chore"

[[content.acceptance_criteria]]
text = "No new child modules are created beyond `mod.rs` replacements and moved module contents remain mechanical"
status = "pending"
category = "chore"

[[content.acceptance_criteria]]
text = "Public crate/module imports and CLI help snapshots remain compatible"
status = "pending"
category = "chore"

[[content.acceptance_criteria]]
text = "`cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, and `govctl check --deny-warnings` pass"
status = "pending"
category = "chore"
