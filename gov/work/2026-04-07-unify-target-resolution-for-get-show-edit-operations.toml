#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-04-07-002"
title = "Unify target resolution for get/show/edit operations"
status = "done"
created = "2026-04-07"
started = "2026-04-07"
completed = "2026-04-07"

[content]
description = "Refactor CLI dispatch around a shared target/operation model: unify get with edit target resolution, simplify write-lock classification onto mutating operations, and delete deprecated JSON artifact edit paths while preserving migration support."
notes = ["Keep legacy JSON reads for validation, display, and migration compatibility even while new RFC and clause writes use TOML locators and serializers."]

[[content.acceptance_criteria]]
text = "get and edit share one target-resolution model"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "write-lock classification follows mutating operations instead of command-name allowlists"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "deprecated JSON artifact edit paths are removed while migrate still supports legacy upgrades"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "cargo test -q, cargo run --quiet -- check, and cargo clippy --all-targets --all-features -- -D warnings pass"
status = "done"
category = "chore"
