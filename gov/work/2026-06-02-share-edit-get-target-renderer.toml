#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-02-005"
title = "Share edit get target renderer"
status = "done"
created = "2026-06-02"
started = "2026-06-02"
completed = "2026-06-02"
refs = ["ADR-0031"]

[content]
description = "Reduce duplicated edit get target rendering by sharing the ResolvedTarget-to-runtime rendering path between JSON and TOML edit targets while preserving JSON nested-target rejection and TOML nested-path rendering."

[[content.acceptance_criteria]]
text = "JSON and TOML edit get paths share ResolvedTarget rendering helper code"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "JSON get still rejects nested targets and TOML get still renders nested targets"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Edit get tests, cargo test, clippy, and govctl check pass"
status = "done"
category = "chore"
