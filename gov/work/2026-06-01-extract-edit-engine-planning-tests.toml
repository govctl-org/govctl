#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-102"
title = "Extract edit engine planning tests"
status = "queue"
created = "2026-06-01"
refs = ["ADR-0031"]

[content]
description = "Move edit engine planning tests from the engine module root into a sibling test module. This keeps the engine root focused on planning API and canonicalization logic without splitting the coherent production engine surface."

[[content.acceptance_criteria]]
text = "edit engine planning tests live in src/cmd/edit/engine/tests.rs"
status = "pending"
category = "changed"

[[content.acceptance_criteria]]
text = "edit engine module root keeps production planning logic without inline test bulk"
status = "pending"
category = "changed"

[[content.acceptance_criteria]]
text = "focused edit engine tests and govctl check pass"
status = "pending"
category = "chore"
