#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-02-062"
title = "Share timeout command transcript formatter"
status = "done"
created = "2026-06-02"
started = "2026-06-02"
completed = "2026-06-02"

[content]
description = "Remove the remaining duplicated govctl command transcript formatter from the verification timeout test helper. Keep timeout process handling local to `tests/test_verify.rs`, but reuse the shared completed-process transcript formatter from `tests/common/mod.rs` so static, dynamic, and timeout command paths render identical output."

[[content.acceptance_criteria]]
text = "Verification timeout helper uses shared command transcript formatting"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "test_verify and representative command transcript tests pass"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "fmt, clippy, cargo test, and govctl check pass"
status = "done"
category = "chore"
