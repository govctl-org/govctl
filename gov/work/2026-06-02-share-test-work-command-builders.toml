#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-02-149"
title = "Share test work command builders"
status = "done"
created = "2026-06-02"
started = "2026-06-02"
completed = "2026-06-02"
refs = [
    "RFC-0002",
    "RFC-0006",
]

[content]
description = "Move generic integration-test command builders for dynamic govctl commands and work-item operations into the existing common command helper module. Keep loop-specific helpers in loop_helpers, reuse the generic builders in changelog tests, and avoid new test files or include-based splitting."

[[content.acceptance_criteria]]
text = "Generic dynamic command and work-item test builders live in tests/common/commands.rs instead of loop_helpers"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Changelog integration tests reuse shared work command builders for work new/add/tick flows"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Loop tests keep loop-specific helper semantics while importing generic work builders from common commands"
status = "done"
category = "changed"

[[content.acceptance_criteria]]
text = "Focused changelog and loop tests plus govctl check pass with no include-based test splitting"
status = "done"
category = "chore"
