#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-080"
title = "Remove stale theme dead-code allowance"
status = "queue"
created = "2026-06-01"

[content]
description = "Remove the broad module-level `dead_code` allowance from `src/theme.rs` now that the semantic color helpers are wired into CLI list/status output and TUI rendering. Keep the shared theme API intact and avoid splitting the coherent theme module."

[[content.acceptance_criteria]]
text = "`src/theme.rs` no longer has a module-wide `dead_code` allowance"
status = "pending"
category = "removed"

[[content.acceptance_criteria]]
text = "The shared semantic theme API remains in `src/theme.rs` without creating new modules"
status = "pending"
category = "chore"

[[content.acceptance_criteria]]
text = "`cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, and `govctl check --deny-warnings` pass"
status = "pending"
category = "chore"
