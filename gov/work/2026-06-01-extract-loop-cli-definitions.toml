#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-06-01-093"
title = "Extract loop CLI definitions"
status = "queue"
created = "2026-06-01"
refs = ["RFC-0006"]

[content]
description = "Move the loop CLI subcommand enum and its help text out of the root CLI module so src/cli/mod.rs keeps the top-level command shape while loop-specific argument definitions live in a focused module."

[[content.acceptance_criteria]]
text = "LoopCommand is defined in a focused src/cli child module and re-exported through the existing cli facade"
status = "pending"
category = "changed"

[[content.acceptance_criteria]]
text = "Root CLI module no longer carries loop subcommand variant bodies"
status = "pending"
category = "changed"

[[content.acceptance_criteria]]
text = "loop CLI help snapshots pass"
status = "pending"
category = "chore"
