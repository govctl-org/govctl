[project]
name = "govctl"
default_owner = "@govctl-org"

[paths]
gov_root = "gov"
docs_output = "docs"

[schema]
version = 1

[source_scan]
# Enable source scanning in projects that use govctl governance references
enabled = false
roots = ["src"]
exts = ["rs", "md"]
