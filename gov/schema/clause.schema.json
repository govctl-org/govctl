{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://phaseos.dev/schema/clause.json",
  "title": "Clause Specification",
  "description": "JSON schema for clause definition files",
  "type": "object",
  "required": ["clause_id", "title", "kind", "text"],
  "properties": {
    "clause_id": {
      "type": "string",
      "pattern": "^C-[A-Z0-9-]+$",
      "description": "Unique clause identifier (e.g., C-PHASE-ORDER)"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable clause title"
    },
    "kind": {
      "type": "string",
      "enum": ["normative", "informative"],
      "description": "Clause type"
    },
    "status": {
      "type": "string",
      "enum": ["active", "deprecated", "superseded"],
      "default": "active",
      "description": "Clause lifecycle status"
    },
    "text": {
      "type": "string",
      "minLength": 1,
      "description": "Clause content (Markdown)"
    },
    "anchors": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Cross-reference anchors"
    },
    "superseded_by": {
      "type": ["string", "null"],
      "description": "ID of clause that supersedes this one"
    },
    "since": {
      "type": ["string", "null"],
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Version when this clause was introduced"
    }
  },
  "additionalProperties": false
}
