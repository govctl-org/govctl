<!-- GENERATED: do not edit. Source: RFC-0006 -->
<!-- SIGNATURE: sha256:0b4f6e672b1b6b39e0f32115e31cc9034a31561ed5f60c4a7192579a3da0ae03 -->

# RFC-0006: Loop Execution Model

> **Version:** 0.1.0 | **Status:** draft | **Phase:** spec

---

## 1. Summary

### [RFC-0006:C-SUMMARY] Summary (Informative) <a id="rfc-0006c-summary"></a>

This RFC defines the **loop execution model** for driving work items to completion through iterative rounds.

A loop is a first-class execution concept that orchestrates one or more work items through implementation, verification, and refinement cycles. Loops support:

- **Single work item execution**: Drive one work item through rounds until acceptance criteria are satisfied
- **Multi-work item execution**: Drive multiple work items with dependency resolution, executing them in topological order
- **Failure propagation**: When a work item fails, dependent work items are automatically blocked
- **Resumption**: Interrupted loops can resume from their last recorded state

The loop model maintains clear separation between:
- **Work items**: Durable governance artifacts tracking what needs to be done and why
- **Loop state**: Ephemeral execution state tracking progress through rounds

This separation ensures work items remain clean outcome artifacts while enabling rich execution traceability for debugging and resumption.

Loops interact with work items exclusively through `govctl work` commands, ensuring all status transitions are validated and auditable per RFC-0001 lifecycle rules.


> **Tags:** `core`

*Since: v0.1.0*

---

## 2. Specification

### [RFC-0006:C-LOOP-DEFINITION] Loop Definition (Normative) <a id="rfc-0006c-loop-definition"></a>

A **loop** is an execution session that drives one or more work items to completion through iterative rounds of implementation, verification, and refinement.

Each loop MUST:
1. Operate on an explicit, finite set of work items specified at loop start
2. Resolve work item dependencies before execution begins
3. Track execution state independently of work item files
4. Terminate when all work items reach terminal state or a failure condition is met

A loop MUST NOT:
1. Modify work item files directly (MUST use `govctl work` commands for status transitions)
2. Persist execution trace to work item fields; execution trace belongs in loop state
3. Assume a specific execution order (sequential or parallel) — downstream implementations choose

Rationale: Loops are a first-class execution concept that orchestrate work item completion. By keeping loops separate from work item files, we maintain clear separation between execution state (ephemeral) and governance artifacts (durable). Work item `notes` hold durable constraints and learnings that should survive future work; loop state holds execution trace. Using `govctl work` commands ensures all status transitions are validated and auditable.


> **Tags:** `core`

*Since: v0.1.0*

### [RFC-0006:C-LOOP-LIFECYCLE] Loop Lifecycle (Normative) <a id="rfc-0006c-loop-lifecycle"></a>

A loop MUST have exactly one of the following lifecycle states:

1. **pending** — Initial state. The loop is defined but execution has not started.
2. **active** — The loop is currently executing rounds on work items.
3. **completed** — All work items reached terminal state (done or cancelled).
4. **failed** — A failure condition was met (max rounds exceeded, critical error).

Valid transitions:
- pending → active (loop execution begins)
- active → completed (all work items finished successfully)
- active → failed (failure condition met)
- active → pending (loop paused for resumption)

Invalid transitions (MUST be rejected):
- completed → any (terminal state)
- failed → any (terminal state)
- pending → completed (cannot complete without being active)
- pending → failed (cannot fail without being active)

Rationale: The loop lifecycle provides clear semantics for tracking execution progress and enables resumption after interruption. Terminal states (completed, failed) indicate the loop has finished and cannot be restarted.


> **Tags:** `lifecycle`, `core`

*Since: v0.1.0*

### [RFC-0006:C-DEPENDENCY-SEMANTICS] Work Item Dependency Semantics (Normative) <a id="rfc-0006c-dependency-semantics"></a>

Work items MAY declare dependencies on other work items using the `depends_on` field in the `[govctl]` section. The `depends_on` field MUST contain a list of work item IDs.

**Dependency rules:**

1. **Acyclicity**: The dependency graph MUST be acyclic. Implementations MUST detect cycles at loop start and reject the loop with a clear error message identifying the cycle.

2. **Blocking semantics**: A work item MUST NOT begin execution until all its dependencies reach terminal state (done or cancelled).

3. **Outcome checking**: After all dependencies reach terminal state, the loop MUST check their outcomes:
   - If all dependencies are `done`: the dependent work item MAY proceed with execution
   - If any dependency is `cancelled` or `failed`: the dependent work item MUST be marked as `blocked` and MUST NOT be executed

4. **Distinction from refs**: The `depends_on` field is distinct from `refs`:
   - `refs`: informational cross-references to related artifacts (RFCs, ADRs, work items)
   - `depends_on`: blocking execution dependencies (work items only)

5. **Failure propagation**: When a work item is marked as `blocked`, all work items that depend on it (directly or transitively) MUST also be marked as `blocked`.

**Loop-level status tracking**: The `blocked` and `failed` states are loop-level execution statuses tracked in loop state, not work item `status` field values. Work items maintain their own lifecycle states (queue, active, done, cancelled) per RFC-0001:C-WORK-STATUS.

Rationale: Dependencies enable complex workflows where work items must be executed in a specific order. By keeping `depends_on` separate from `refs`, we maintain clear semantics: `refs` are informational, `depends_on` are blocking. Loop-level status tracking avoids polluting the work item status field with execution-specific states.


> **Tags:** `lifecycle`, `validation`, `work-items`

*Since: v0.1.0*

### [RFC-0006:C-ROUND-EXECUTION] Round Execution (Normative) <a id="rfc-0006c-round-execution"></a>

Each work item is processed through iterative **rounds** until termination.

**Round structure:**

Each round MUST:
1. Read the work item (description, acceptance criteria, refs, notes)
2. Implement changes addressing pending acceptance criteria
3. Run verification guards
4. Evaluate acceptance criteria (ticked, still pending, or failed)
5. Record loop execution state (what was done, what changed, why) outside the work item file
6. Decide: continue (pending criteria remain) or terminate (all done or failed)

**Termination conditions:**

A work item MUST terminate when:
- **Success**: All acceptance criteria are satisfied and all required guards pass
- **Failure**: Any of the following conditions are met:
  - Maximum rounds reached (implementation-defined limit)
  - Critical guard failure (guard execution error, not guard check failure)
  - Agent decides unable to proceed (e.g., missing information, architectural conflict)

**Post-termination:**

- On success: The work item MUST transition to `done` via `govctl work move`
- On failure: The work item remains in `active` state but is marked as `failed` in loop state
- Failed work items trigger failure propagation per C-DEPENDENCY-SEMANTICS

Rationale: Rounds provide iterative refinement toward acceptance criteria. The round structure ensures each iteration makes measurable progress without writing execution trace into the work item field surface. Termination conditions prevent infinite loops while allowing implementations to define appropriate limits.


> **Tags:** `core`, `validation`

*Since: v0.1.0*

### [RFC-0006:C-WORK-ITEM-INTERACTION] Work Item Interaction (Normative) <a id="rfc-0006c-work-item-interaction"></a>

Loops interact with work items through status transitions and validation checks.

**Status transitions:**

1. **Before execution**: Work items in the loop set MUST be in `queue` or `active` state
2. **During execution**: The loop transitions work items from `queue` to `active` via `govctl work move`
3. **On success**: The loop transitions work items from `active` to `done` via `govctl work move`
4. **On failure**: Work items remain in `active` state (the loop marks them as `failed` in loop state, but does not change their work item `status` field)

**Validation requirements:**

Before transitioning a work item to `done`, the loop MUST verify:
1. All acceptance criteria are satisfied (per RFC-0001:C-GATE-CONDITIONS)
2. All required verification guards pass (per RFC-0001:C-GATE-CONDITIONS)
3. The work item has been in `active` state during this loop execution

**Durable work item context:**

The work item `notes` field is reserved for **durable context** that should persist across sessions:
- Key decisions made during implementation
- Blockers encountered and how they were resolved
- Important technical insights

Execution trace (round-by-round progress) MUST be tracked in loop state, not in work item fields.

**Loop-level status:**

The loop tracks additional execution status in loop state:
- `pending`: Work item has not started execution
- `active`: Work item is currently being executed
- `done`: Work item completed successfully (matches work item `done` state)
- `failed`: Work item execution failed
- `blocked`: Work item cannot execute due to failed/cancelled dependencies

These loop-level statuses are independent of work item `status` field values (queue, active, done, cancelled).

Rationale: Using `govctl work move` commands ensures all status transitions are validated and auditable. Separating loop-level status from work item status and notes maintains clear boundaries between execution state (ephemeral) and governance artifacts (durable).


> **Tags:** `lifecycle`, `validation`, `work-items`

*Since: v0.1.0*

### [RFC-0006:C-LOOP-RESUMPTION] Loop Resumption (Normative) <a id="rfc-0006c-loop-resumption"></a>

Loops MAY support resumption after interruption (agent session end, user cancellation, system failure).

**Resumption requirements:**

If a loop implementation supports resumption, it MUST:
1. Persist loop state to durable storage (independent of work item files)
2. Detect existing loop state when invoked with the same work item set
3. Resume from the last recorded state rather than starting fresh

**Resumption semantics:**

When resuming a loop:
- Work items in `done` loop state MUST NOT be re-executed
- Work items in `active` loop state MAY be resumed from their last round or restarted
- Work items in `blocked` loop state MUST remain blocked
- Work items in `pending` loop state MUST be executed in dependency order

**State preservation:**

The loop MUST preserve:
- Execution status of each work item (pending, active, done, failed, blocked)
- Dependency graph (if multi-WI loop)
- Round count for each work item

The loop MAY preserve:
- Detailed execution trace (execution entries, round logs)
- Guard execution results
- Agent context and decision history

Rationale: Resumption enables long-running multi-WI loops to survive agent session boundaries without losing progress. By persisting state independently of work item files, we maintain the separation between execution state (ephemeral) and governance artifacts (durable).


> **Tags:** `lifecycle`

*Since: v0.1.0*

---

## 3. Rationale

### [RFC-0006:C-UNIFIED-MODEL] Unified Model Rationale (Informative) <a id="rfc-0006c-unified-model"></a>

The unified loop model (single primitive for both single-WI and multi-WI execution) was chosen over separate `/loop` and `/batch` skills because:

1. **Work items naturally form DAGs**: Agents frequently create multiple related work items with dependencies, then iterate through them as a batch. Separate skills would force users to choose between `/loop WI-001` and `/batch WI-001 WI-002 WI-003` when the underlying mechanism is identical.

2. **Simpler mental model**: "A loop drives work items to completion" is one concept. "A loop drives one work item, a batch drives multiple" is two concepts with overlapping semantics.

3. **Consistent state model**: `.govctl/loops/<loop-id>/` works the same whether the loop contains one work item or ten. Execution logs, state file, and round logs scale naturally.

4. **Downstream flexibility**: The loop does not prescribe sequential or parallel execution. Downstream applications (agents, CI systems) can choose the execution model that fits their constraints.

> **Tags:** `core`

*Since: v0.1.0*

### [RFC-0006:C-EXECUTION-STATE-SEPARATION] Execution State Separation Rationale (Informative) <a id="rfc-0006c-execution-state-separation"></a>

Execution state lives in `.govctl/loops/` (not in work item TOML) because:

1. **Work items are outcome artifacts**: They should contain scope (description), success criteria (acceptance_criteria), and durable context (notes). They should not accumulate execution logs during active work.

2. **Rich execution traceability**: Loops need to track round-by-round progress, guard results, criteria addressed per round, and detailed logs. This is too much data for a work item TOML file.

3. **Clean diffs**: Work item TOML files remain stable and reviewable. Execution state changes frequently during a loop and is not meant for code review.

4. **Ephemeral by design**: Loop state is local to the developer's machine. It can be deleted without losing governance artifacts. The work item's `notes` field captures durable learnings.

> **Tags:** `core`

*Since: v0.1.0*

### [RFC-0006:C-DEPENDENCY-SEPARATION] Dependency Separation Rationale (Informative) <a id="rfc-0006c-dependency-separation"></a>

The `depends_on` field is separate from `refs` because they serve different purposes:

- **`refs`**: Informational cross-references ("this work item is related to that RFC"). Does not block execution. Used for traceability and navigation.

- **`depends_on`**: Execution dependencies ("this work item cannot start until that work item completes"). Blocks execution. Used for dependency resolution and failure propagation.

Conflating these would force the loop to guess whether a ref is a hard dependency or just a related artifact. Separate fields make intent explicit.

> **Tags:** `core`, `work-items`

*Since: v0.1.0*

---

## Changelog

### v0.1.0 (2026-05-31)

Initial draft
